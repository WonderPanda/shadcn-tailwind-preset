import { convertRadixColorToShadTheme } from './radix-colors';

describe('convertColorToTheme', () => {
  it('does stuff', () => {
    const theme = convertRadixColorToShadTheme('sky');
    console.log(theme);
  });
});
